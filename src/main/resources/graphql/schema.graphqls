type Query{
    books: [Book]
    getBookById(id: Int): Book
}

type Book{
    id: ID
    name: String
    genre: String
    pageCount: Int
    author: Author
}

type Author{
    id: ID
    name: String
}